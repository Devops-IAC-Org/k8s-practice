apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-prod-ingress
  namespace: argocd
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - argocd.prod.mycompany.com
    secretName: argocd-server-tls
  rules:
  - host: argocd.prod.mycompany.com
    http:
      paths:
      - path: /argocd
        pathType: Prefix
        backend:
          service:
            name: argocd-server
            port:
              number: 80
